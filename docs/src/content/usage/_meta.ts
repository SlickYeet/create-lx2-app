import { MetaRecord } from "nextra"

import { RenderTitle } from "@/components/navigation/render-title"

const meta: MetaRecord = {
  "first-steps": {
    title: RenderTitle({ label: "First Steps" }),
  },
  payloadcms: {
    title: RenderTitle({ label: "Payload CMS" }),
  },
}

export default meta
